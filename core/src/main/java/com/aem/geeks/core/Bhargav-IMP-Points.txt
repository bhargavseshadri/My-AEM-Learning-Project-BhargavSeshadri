=============================================================================================
QUICK IMPORTANT NOTES

=============================================================================================
IMPORTANT PATHS
-> For Configurations(crx) : apps/system/config, (preferred path is /apps/my-prjt/osgiconfig/config)
-> For Checking the sling Adapters : /system/console/adapters
-> AEM replication agents : /etc/replication/agents.author.html
-> Check the available run modes : /system/console/status-slingsettings
-> To Check the status of the scheduler : /system/console/status-slingscheduler
-> Workflow model usually stored in two places in our jcr
       1. Design Model - Stored Under (crx) : /conf/global/settings/workflow/models
       2. Runtime Model - Stored Under (crx) : /var/workflow/models
       * The WF STEP dialog values are stored inside : /conf/global/settings/workflow/models/bhargav-personal-workflow/jcr:content/flow/geeksworkflowstep/metaData
-> We can find all the available users( and also deactivate the user here) : /security/users.html

-> In Manual System User creation,
    - to create new user : crx/explorer/index.jsp
    - To give the required permissions(ACL's) : /Useradmin
    - Note: Service users are under this path (crx) : /home/users/system, and we cant login in to aem using service user

-> GRAPHIQL EDITOR : http://localhost:4502/aem/graphiql.html
-> To see graphql endpoints in crxde : /content/cq:graphql/global/endpoint
-> to create graphql endpoint : http://localhost:4502/libs/cq/graphql/sites/admin/content/console.html

=============================================================================================
* Important Points

-> Sometimes the new template(static or editable) you have created might not shows up in templates section or while creating a page - in that case
   once go and check the "cq:allowedTemplates" property under "/content/aemgeeks/jcr:content".

-> If your Component won't need any dialog box, (it might fetch the content from API and prints it on page), the in that case we
   won't use CQ:DIALOG, we use CQ:EditConfig.
   - This Cq:EditConig is also just to show our component to drag and drop on page.
   - apps/aemgeeks/components/content/bhargavapicomp/_cq_editConfig.

-> To directly print the given dialog field value on the page(without using Sling Model) use "properties".
        - ${properties.fieldName}
        - Internally it simply means "resource.getValueMap()".

-> sling:hideResource="{Boolean}true" : * To Hide a Core component dialog field in our proxy comp, Add this property to that in the
                                          overriden cq_dialog in proxy comp.
                                        * You have to add that field and use this hideResource option with it by "Matching that core
                                          component field node structure, Then only it works",
                                          EXAMPLE for this is in "apps/aemgeeks/components/bhargavtextproxy/_cq_dialog/.content.xml"

-> * We can have different variations(copies) of our Content Fragment, and to use that we have to pick the variation we want in the CF dialog box on the page.
   * and we can also sync CF variation with CF master as well.

-> In modern AEM versions and AEM as a Cloud Service, you must explicitly configure the path of the folder containing your Content Fragment Models
      in the "Allowed Content Fragment Models" field under the Policies tab of the corresponding folder in /content/dam. Otherwise, the models will not shown up
      when creating Content Fragments in that folder.

-> Very.Imp - In "Servlets" getting a Current Resource.
                * ResourceType Servlet : here we can get the Current Resource path directly, REQUEST will give to us the current resource.
                                         Eg : Resource resource = req.getResource();
                                              String resourcePath = resource.getPath();
                                              Output : /content/aemgeeks/us/en/delete-later-2/jcr:content/root/responsivegrid/registrationformserv

                PathType Servlets : Here the situation is different, if we tried the above code for it, like if we try to get the
                                    resource using request it will give the path we have given in the code, eg: "/bin/bhargav".
                                    because the request is not associated with a content resource.

=============================================================================================
USING a Model, Service, HTL in NORMAL JAVA CLASS

1. Can we use a Java class in HTL
Ans. Directly we cannot use a Java class in HTL, but we can use it through Sling Model.
        - Write the logic in JAVA CLASS --> Use it in sling model by creating OBJ --> Then use it in HTL with the help of model.

--------------------------------------------------
2. Can we use a Service in JAVA Class?
Ans. - we cant use it directly using @Reference
     approach-1: Using Constructor:  Instead we can send the service as a parameter to the JAVA class "constructor" and then we can use it in that class.
              -- Java Service --> Sling model - send the service in obj constructor --> Use the service in JAVA class
              EG : Model : com/aem/geeks/core/models/impl/BhargavModelServiceClassTestingModelImpl.java
                   Service : BhargavModelServiceClassTestingServiceImpl.java
                   Java Class : BhargavModelServiceClassTestingModelImpl.java

    approach-2: Using Bundle Context : The BundleContext queries the OSGi service registry and returns the registered service.
                -- Service class have the logic --> Java class gets hold of service using Bundle context --> sling model uses the java class using obj
                EG: Service : BhargavModelServiceClassTestingServiceImpl.java
                    Java Class : BhargavServiceInJava.java
                    Model: BhargavModelServiceClassTestingModelImpl.java

--------------------------------------------------
3. Can we use a sling model in java clas ?
Ans. Directly we cant use, Indirectly we can use but it is discouraged.
        Approach - 1. Similar to the above example, here also we send the resource to java class using Constructor or as a method parameter.
                      and then use it in the java class.
                        EG :  ArticleModel model = resource.adaptTo(ArticleModel.class);
                              or from a request
                              ArticleModel model = request.adaptTo(ArticleModel.class);

        Approach - 2. We have to send the sling model as a object through a constructor or through a method
                      Eg: Sling model : @Model(adaptables = Resource.class)
                                        public class MySlingModel {

                                            @ValueMapValue
                                            private String title;

                                            public String getTitle() {
                                                return title;
                                            }

                                            public String getFormattedTitle() {
                                                MyUtil util = new MyUtil();
                                                return util.format(this);  //"this" represents to the current clas object.
                                            }
                                        }

                          Java Class : public class MyUtil {

                                           public String format(MySlingModel model) {  //here we are getting that obj and using it
                                               return model.getTitle();
                                           }
                                       }

--------------------------------------------------
4. Can we use a Service directly in HTL?
Ans. no we cannot use it directly, we need a sling model.

=============================================================================================
*) Logger Levels
        Trace
        |
        Debug
        |
        Info
        |
        Warn
        |
        Error

* For Eg: If you give log level as "Info" for the log file, so in that file "Trace & Debug" logs wont print, "Info, Warn, Error"
          logs will print in that file.

=============================================================================================
*) DATA-SLY-TEMPLATE & DATA-SLY-CALL

- A data-sly-template is simply a reusable block of HTL code that can accept parameters.
- We must use these three combined to create a reusable template and call it with different parameters.
            data-sly-template = Define reusable HTML
            data-sly-use = this is used to import that defined template file
            data-sly-call = Call that reusable HTML

- Reference Code
-> data-sly-template : apps/aemgeeks/components/content/bhargav-compmultifield/bhargav-reusable-code.html
          * data-sly-template.compMultifieldTemplate="${@ itemNeeded, bhargavCount}", here we have created "compMultifieldTemplate" template
            which takes two parameters.

-> data-sly-use : apps/aemgeeks/components/content/bhargav-compmultifield/bhargav-compmultifield.html
          * data-sly-use.bhargavHtlTemplate="bhargav-reusable-code.html", here we are importing the above defined template file.

-> data-sly-call : apps/aemgeeks/components/content/bhargav-compmultifield/bhargav-compmultifield.html
          * data-sly-call="${bhargavHtlTemplate.compMultifieldTemplate @ itemNeeded = item, bhargavCount = itemList}",
                - here we are calling the above defined template and passing the parameters to it and using it.

------------------------------------
Difference between data-sly-template and data-sly-call?

* Data-sly-template
    - Reusable HTML block inside an HTL file
    - Can accept parameters

* Data-sly-include
    - Includes another HTL file
    - Similar to copying another file into the current one

=============================================================================================
*) (HTL) Global Objects available in HTL (most common ones)

-> Enumerable Objects : These objects provides a "VALUEMAP" which contains the information, we can access that info.
- properties : Provides the component properties, like whatever we save through dialog, we can access those props without using sling model.
- pageProperties : This will provide you the properties or info of page where our component used, In simple words getting page informatuion
- inheritedPageProperties : It provide the inherited properties of page, we can access the properties of the parent page

-> Demo code available here: apps/aemgeeks/components/content/author-books/author-books.html

---------------------------------------
-> (HTL) Java-Backed-Object
- Component : Provides the information of that component.
- componentContext : It is open for each conmponent, Independent of wcmmode.
- resource : object of current resource(when we drag and drop a comp on page, then the created node of that comp in that page is called as current Resource).
- resourcePage : On the page where we drag and drop the component, It provides the information of that page means the current page
- currentDesign : there is a design node available to store the template level properties, that info we can get through this. if nothing is set
                  then it gives the default libs design node path
- currentNode : when we drag and drop a component on a page, then that comp node is treated as a current node.
- currentPage
- resourceDesign : it is the same as the current design
- wcmmode : It provide you the information, that in which mode you are, returns "boolean", for EDIT: isEdit method, for Preview: isPreview method.

-> Demo code available here: apps/aemgeeks/components/content/author-books/author-books.html

=============================================================================================
*) MANDITORY METHODS

a) Sling Models : No manditory methods
b) Services : No manditory methods
c) Servlets :
        - ResourceType : No manditory Methods, but based on the requirement we will use doGet, doPost, doPut, doDelete
        - PathType     : No manditory Methods, but based on the requirement we will use doGet, doPost, doPut, doDelete

d) Configurations:
        - Normal   : No manditory method, but have to define "activate" method to get the config values
        - Factory Configuration : a) Activate - not a manditiory, but have to define for getting the config values.
                                  b) BIND Method
                                  c) UnBind Method

e) Schedulers : we have two types of schedulers 1) Runnable, 2)Job Scheduler
                1)Runnable : RUN() method is the manditory one. "activate method", Not a manditory method, but to get the config values we define it
                2)Job  : execute(JobContext jobContext) is the manditory method, "activate method", Not a manditory method,
                         but to get the config values we define it.

f) Workflow :
            1) Custom WF Process : execute(WorkItem workItem, WorkflowSession workflowSession, MetaDataMap processArguments)

g) Custom Live Action(for custom rollout configurations) : 1) LiveActionFactory : createsAction(), createAction(Resource resource)
                                                           2) LiveAction : getName(), execute(Resource source, Resource target, LiveRelationship relation,
                                                                                              boolean autoSave, boolean isResetRollout)

=============================================================================================
*) @Component

--> Servlets : @Component(service = Servlet.class)
--> OSGI Configuration Service : @Component(service = bhargavOSGiConfigService.class,immediate = true)
--> Schedulers :
                - Runnable : @Component(immediate = true, service = Runnable.class)
                - Job :  @Component(immediate = true, service = Job.class)

--> Service : @Component(Service = bhargavService.class)  - bhargavService is my interface and I am linking it to my implimentation class using @Component
--> Workflow : @Component(service=workflowProcess.class)
--> Custom Live Action Factory : @Component(service = LiveActionFactory.class)

---------------------------------------
** @Component - Properties we can have in @Component annotation
1. service =
2. immediate = true
3. configurationPolicy = ConfigurationPolicy.OPTIONAL
                                            .REQUIRE
                                            .IGNORE
4. property = {"service.vendor=AEM GEEKS",
   		       "service.description=AEM GEEKS Training" }
   		       * This property is also used to filter the service we want to inject in the another sercvice
   		            Eg: Suppose we have two services
                    1) @Component(service = BhargavService.class, property = "country=US")
                            public class USPaymentService implements BhargavService {

                    2) @Component(service = BhargavService.class, property = "country=IN")
                            public class IndiaPaymentService implements BhargavService {

                    * Now we want to inject only the first service in another service, then we can use this property to filter it.
                    Eg: @Reference(target="("country=US")")
                        private BhargavService bhargavService;

5. name = "com.aem.geeks.core.services.ComponentsInfo" - This gives that OSGi component a unique name.
4. configurationPid = "com.aem.geeks.core.services.impl.ComponentsInfoImpl"  - this is often not required, aem will automatically gets the osgi configuration and
                                                                                link it to our service.

=============================================================================================
*) ANNOTATIONS:

@Activate : when we want something to load or execute at the time of that particular service initializations, there we use this. for loading init type method.

@PostConstruct : A method with this annotation is called after all the injections are done. It is mainly used for initialization logic.

- immediate=true When we want to activate the service immediately after the bundle gets started in that place we use this.

@Modified : It is a lifecycle callback method annotation used in OSGi components. It marks a method that should be called
            whenever the OSGi configuration for that component is changed at runtime, without restarting the bundle.
- In simple words, When you change configuration values, AEM will automatically re-invoke the method annotated with @Modified, So your service picks up
  the new configuration immediately — without redeploying or restarting AEM.

@ValueMapValue : Specifically used to get the node properties of the resource. for access node props, this is safer and cleaner than @inject.

@Inject : Inject values from AEM resource in to model fields. it is more flexible and injects almost anything like
          requestAttributes, Resource Props, Resource, ResourceResolver, Request etc.

@Default : provides a default value if the actual comp dialog field value is not given.

@SlingObject : Injects Sling objects such as ResourceResolver, Resource, SlingHttpServletRequest.

@OSGiService : Injects an OSGi service into the Sling Model.

@Reference : Injects an osgi service in to another osgi service.

@ScriptVariable : Used in aem to get built-in objects from the htl script, like currentPage, resource, PageManager

@Component : Marks a class as an OSGi service component. used for registering our service in osgi.

@ObjectClassDefinition(name, definition) : used for defining osgi configuration.

@AttributeDefinition : used for defining fields in the configuration.

@Designate (ocd = BhargavOSGiFactoryConfig.class, factory = true) : Links configuration class to the component (used for OSGi configs).

@Configuration : Used for craeting Context-Aware Configuration, which is different from OSGi configuration.

@ChildResource : @ChildResource in Adobe Experience Manager Sling Models is used to inject child resources (nodes) of the current resource directly
                 into your model—instead of you manually traversing the JCR tree.
                Eg : @ChildResource(name = "multifieldName")
                     private List<ChildModel> items;

@ResourcePath(path="/content/aemgeeks/us/en/home") : - Instead of injecting the current resource, Sling fetches the resource located at the given path
                                                     - This simply means : Resource resource = resourceResolver.getResource("/content/we-retail/us/en");

@RequestAttribute(name = "rAttribute") : - It injects an attribute stored in the HTTP request.
                                         - It simply means request.getAttribute("rAttribute")
                                         - it is used to get a value that is passed only for that specific request.

-------------------------
@Self & @Via

Note : First understand the situation
* A Sling Model is created using something called an adaptable.
* @Model(adaptables = SlingHttpServletRequest.class)
        - Sling must sometimes be told where to take data from.

* When Sling creates the model:
  - It uses the adaptable (request or resource) to construct the model
  -(Imp) Then it processes every field separately
  -(Imp) Each field needs its own injection instruction
  - adaptables: means we're saying to sling that this model can be created using a request or resource. Mainly defines how model is created.
  - @Self: using this we say that store that same request or resource inside this variable. injects that adaptable into a field



@Self : It tells Sling to create this model from the current adaptable (Request or Resource). Give me the same object that created the model.
  - adaptables: means we're saying to sling that this model is created using a request or resource. Mainly defines how model is created.

  - @Self: using this we say that store that same request or resource inside this variable. injects that adaptable into a field.
  - IMP : This @Self says: "don't go look at a child resource, don't call an OSGi service, don't read a value map property — just take the current adaptable itself."
          just adapt the same resource/request you're currently sitting on.


- Work of @Self is :
        -> @Self
           private Resource resource;  this means -- resource = adaptable.adaptTo(Resource.class);

        -> @Self
           private SlingHttpServletRequest request;  This means -- request = adaptable.adaptTo(SlingHttpServletRequest.class);

        -> @Self
           private Teaser teaser;  This means -- teaser = adaptable.adaptTo(Teaser.class);


Eg : @Model(adaptables = SlingHttpServletRequest.class)
     public class MyModel {

         @Self
         private SlingHttpServletRequest request;

         public String getPath() {
             return request.getRequestPathInfo().getResourcePath();
         }
     }
 - If @Self is removed: private SlingHttpServletRequest request;   ---> then request will be null, even though the model was created using the request.

-----------------------------------------------------

@Via : Before injecting, go through this object first.
* It is mainly used because most AEM models are adaptable from request, but properties are stored in resource.

Eg : Model created using request, but property is inside resource.
@Inject
private String title;  // this will mostly gives null

@Inject
@Via("resource")       // This fixes the issue and get the props by looking in component resource properties.
private String title;

* Here the flow is
    - Start with request.
    - Go to request.getResource().
    - Then read property title.

=============================================================================================
*) req.setAttribute() AND req.getAttribute()  Explanation with Example

Q) What is an “attribute” in a request?
Answer: You can store temporary data in the HttpServletRequest object using setAttribute() and later read it back using getAttribute().
--> Think of a HttpServletRequest (or SlingHttpServletRequest) object as a container or bag that travels from the
    browser → AEM → servlet → JSP → other servlets.
--> That bag can carry
a) Parameters (from the user input, like req.getParameter("name"))
b) Headers (like cookies, content-type)
c) And also... attributes that developers can manually add.

--> So attributes are temporary data stored inside the request object — for this single request only.
--> req.setAttribute("key", value);  => Adds a data item to the request.
--> req.getAttribute("key");  => Retrieves that data item later.

--> Example With code snippets: Imagine you’re processing a registration form.
Step 1: You receive a POST request.
String name = req.getParameter("name");
String email = req.getParameter("email");

- Now, before forwarding this to a JSP page or another servlet, you want to store these values inside the request temporarily

Step 2: Set attributes
req.setAttribute("userName", name);
req.setAttribute("userEmail", email);
- Now your req object carries these attributes.

Step 3: Forward the request
RequestDispatcher dispatcher = req.getRequestDispatcher("/thankyou.jsp");
dispatcher.forward(req, res);

- Then inside your JSP (HTL or JSP):
<p>Welcome ${requestScope.userName}! Your email is ${requestScope.userEmail}</p>  --> Output : Welcome Bhargav! Your email is bhargav@example.com

Final understanding: (temporary notepad that travels along with one request) Here with in the request, until the req is there at that time we are setting
these attr(variables) userName, userEmail with our data.

Note: We can forward this attributs from one servlet to another, like we can set these attributs in one servlet and then get these in another servlet
// Forward the request to another servlet
RequestDispatcher dispatcher = req.getRequestDispatcher("/second");
dispatcher.forward(req, res);

=============================================================================================
*) MULTIFIELD - COMPOSITE PROPERTY (composite="{Boolean}true")
- we will add this property while defining a multifield in content dialog, by default it is false
- FALSE : In this case, if we give two set of values to the multifield in the comp dialog, then it store those two sets values in a "STRING ARRAY" inside component node.
- TRUE : In this case if we provide two sets of values to the multifield in the comp dialog, it will create a node under our component node(with name we have give
          for multifield in xml) and then under that node it will create a two item nodes for those two sets of values.
- we use false when we have a single field in the multifield, and true when we have a multiple fields in the multifield.
-> We can render the multifield values using two ways one is using MAP and the other way is using JAVA BEAN

=============================================================================================
*) Sling Model Content Exporter (Content as a Service)
- AEM comes up with OOTB content exporter functionality, means AEM can export content to third party systems in form of JSON or XML.

=============================================================================================
*) Filter.XML - Important things to know about Filter.XML

- Every Module/bundle have its own filter.xml file, filter.xml tells the content package which JCR paths to include when deploying to AEM and
  What content should be installed from this package. if not included then our AEM will simply ignore the deployment of those paths even
  if the files exist in your project.
- Whatever the path we have added in it will be deployed to the server at the time of deployment.,
- If you added any new file or folder, then you must add that path in the filter.xml file, otherwise that file or folder won't be deployed to the server.

--> Different Types of mode in filter.xml
1. replace (default) : Replace CRX repository content with code package content after deployment. it will Delete missing nodes.
                       eg:  - Crx repo have - /apps/myproject/a
                                              /apps/myproject/b
                            - and in our code we have or (we can also call in packaged content) - /apps/myproject/a
                            - Then after deploymet it will delete "/apps/myproject/b" in AEM instance CRX, because it is missing in our code.

2. merge : - It merges code package content with CRX repository content after deployment. it will NOT delete missing nodes.
            eg:  - Crx repo have - /apps/myproject/a

                 - and in our code we have - /apps/myproject/b
                 - Then after deploymet it will NOT delete "/apps/myproject/a" in AEM instance CRX, it simply creates or updates /apps/myproject/b and keep both nodes.
                    /apps/myproject/a   -> remains
                    /apps/myproject/b   -> created

3. Update : - It updates existing nodes in CRX repository with code package content after deployment. it will NOT delete missing nodes and also won't create new nodes.
            eg:  - Crx repo have - /apps/myproject/a
                                   /apps/myproject/b
                 - and in our code we have - /apps/myproject/c
                 - Then after deploymet it will NOT delete anything, it simply adds /apps/myproject/c in aem crx
                    /apps/myproject/a
                    /apps/myproject/b
                    /apps/myproject/c

4. replace_properties : It replaces only the properties of existing nodes in CRX repository with code package content after deployment.
                        it will NOT delete missing nodes and also won't create new nodes.
            eg:  - Crx repo have - /apps/myproject/a (prop1=value1)
                                   /apps/myproject/b (prop2=value2)
                 - and in our code we have - /apps/myproject/a (prop1=newValue)
                 - Then after deploymet it will NOT delete anything, it simply updates the prop1 value of /apps/myproject/a in aem crx
                    /apps/myproject/a (prop1=newValue)
                    /apps/myproject/b (prop2=value2)

=============================================================================================
*) Important Points while integrating with external API's
- Api will not return the data smoothly everytime, some times it fails.

* What if the API fails to return data?
      - The API is another application. That application may have: server crash, database crash, deployment issue, internet issue, authentication issue.
      - Some time the API may give 500 Internal Server Error.
      - So it is always better to check the status code : if(status == 200) and then read the response of the api.
      - Write the code based on the status codes
            Eg: int status = response.getStatusLine().getStatusCode();
                switch(status){

                case 200:                   // all fine
                    //Process

                case 404:                   // Requested resource doesn't exist. may be the requested data is not there
                    //Return empty

                case 401:                   // Authentication faile, maybe token is expired. and 403 is forbidden error means the login is successful but we are not allowed
                    //Refresh token

                case 500:                   // Server crashed or unavailable
                    //Retry

                }

---------------------------------------------
* Retrying the requests

- For example one time the API returns 500 Internal Server Error, that means Maybe Server restarted, Database temporarily unavailable, Thread pool exhausted
   ,Temporary infrastructure issue, These problems are often temporary. so in these cases sending a request after 2 seconds may give successful response.

- In case of 400, 401, 403,404 Retry is useless.
- we do a retry in this times -> 500,502,503,504,429,SocketTimeoutException,ConnectTimeoutException.
- And we shouldn't retry immediately also. we have to wait for some time before a retry.

* Recommended Retry concepts for aem :
        1. Apache HttpClient Retry Strategy : Suppose your AEM service is calling an external API. But before the request reaches the server, the network breaks.
                                              and there is no HTTP response is received. There is no status code like 500 or 404.
                                              - Now "client.execute(request);" throws an exception like
                                                    1. java.net.SocketException
                                                    2. java.net.NoRouteToHostException
                                                    3. java.net.SocketTimeoutException
                                              - Now Apache HttpClient comes in and it will automatically retry these I/O exceptions.

                                              - We receive these exception in case of DNS failure, Connection reset, Socket closed, Temporary network issue.
                                              - It wont retry 500 Internal Server Error

          Coding Example : HttpRequestRetryStrategy retryStrategy = new DefaultHttpRequestRetryStrategy( 3, TimeValue.ofSeconds(2) );
                           CloseableHttpClient client = HttpClients.custom()
                                                                   .setRetryStrategy(retryStrategy)
                                                                   .build();
                            Maximum retries : 3
                            Wait between retries : 2 second

        2. Resilience4j Retry (Recommended)
                - This is what you'll commonly see in enterprise Java applications.
                - Instead of retrying only exceptions, it can retry exceptions, HTTP status codes, custom business conditions.

---------------------------------------------
* Understanding Timeouts:
        - API tries to make a request, it will wait for some time and after that it stops requesting, this is called timeout.
        - There are 3 different timeouts.
               1. Connect Timeout : when AEM trying to establish the connection with api this timeout comes in and it tells AEM how much time AEM have to try to
                                    establish connection.
                                    - This timeout is before the connection is established.
                                    - For example if we give 5 seconds, and after 5 seconds connection isn't established, then an exception will be thrown. and upto that
                                      5 seconds "client.execute()" tries to establish the c
                                    - We specify this using : .setConnectTimeout(5000)
                                    Eg:     private RequestConfig createRequestConfig() {
                                                return RequestConfig.custom()
                                                        .setConnectTimeout(CONNECTION_TIMEOUT_MS)
                                                        .setSocketTimeout(READ_TIMEOUT_MS)
                                                        .build();
                                            }

                2. Socket Timeout : After Connection succeeded. Now API starts processing. and this timeout tell how long our aem have to wait for the data. for
                                    example if we give 5 seconds. then after 5 sec timeout happens.
                                    - This timeout is after the connection is established. request reaches server. Server starts processing. AEM now waits for data.
                                      that waiting time is specifid in this socket timeout.
                                    - After that timeout also still the response is delayed then SocketTimeoutException

                3. Connection Request Timeout : This exists only if you're using a connection pool.
                   - connection pool : A connection pool is a set of already-open network connections that your application reuses instead of creating a new
                                       connection for every API request. When you're calling external APIs (REST, GraphQL, etc.), opening a new TCP connection
                                       is relatively expensive. A connection pool keeps connections alive so they can be reused.
                                       - Without a connection pool, Imagine your application needs to make 100 API requests. You're paying the connection setup cost 100 times.
                                       - With a connection pool Instead we create 10 connections, The connections stay open and are reused.

                                       Eg : Create 10 connections
                                            Pool:
                                            Connection 1
                                            Connection 2
                                            ...
                                            Connection 10

                                            Request 1 -> Connection 1
                                            Request 2 -> Connection 2
                                            Request 3 -> Connection 1 (reused)
                                            Request 4 -> Connection 5
                                            ...

                                            Code example : protected void activate() {
                                                                   PoolingHttpClientConnectionManager manager = new PoolingHttpClientConnectionManager();
                                                                   manager.setMaxTotal(20);
                                                                   manager.setDefaultMaxPerRoute(10);

                                                                   httpClient = HttpClients.custom()
                                                                           .setConnectionManager(manager)
                                                                           .build();
                                                               }

                                                               public String getProduct(String id) throws IOException {
                                                                    HttpGet get = new HttpGet("https://api.company.com/products/" + id);

                                                                       try (CloseableHttpResponse response = httpClient.execute(get)) {
                                                                           return EntityUtils.toString(response.getEntity());
                                                                       }
                                                               }
                                                           }

- Now comeback to our concept connection request timeout : so according to above code we have 10 connections, and when a new request comes in it have to wait till one
                                                           connection becomes free, that waiting time will be defined by this connection request timeout.
                                                           eg: RequestConfig config = RequestConfig.custom()
                                                                                               .setConnectionRequestTimeout(3000)
                                                                                               .build();


---------------------------------------------
* Headers: Headers are simply extra information that travels along with the HTTP request.
           - request.addHeader("Authorization", "Bearer " + token);

=============================================================================================
*) Asset API --> Asset asset = resource.adaptTo(Asset.class);

- By doing this "Asset asset = resource.adaptTo(Asset.class);", we are converting the generic resource into an AEM DAM Asset API object
  that provides higher-level methods.

- for example:
* asset.getMetadataValue("dc:title")
* asset.getOriginal()
* asset.getRenditions()
* asset.addRendition(...)
* asset.getMimeType()

=============================================================================================
*) PAGE MANAGER(Aem's API) & Page

Page → where you are
pageManager → go anywhere

==> PageManager
- PageManager is a service API in AEM used to: find pages, create pages, move/delete pages, resolve paths → pages.
- Its like a tool. Fetch/manage pages
- Used in this file : com/aem/geeks/core/models/impl/BhargavBackendAemApisCompModelImpl.java


==> Page (just a object) : Page is a Page object, It represents one specific page. And gives its properties, title, path, etc.
- It just Read info of the current page.
- Used in this file : com/aem/geeks/core/models/impl/BhargavBackendAemApisCompModelImpl.java

Eg :
- Using Page Maneger getting the Page.

PageManager pageManager;
Page page=pageManager.getPage("/content/aemgeeks/us/en");
- Using Page getting the page info : String title = page.getTitle();

=============================================================================================
*) adaptTo()

* It is simply like a variable converting thing, eg: Node node = resource.adaptTo(Node.class);  -> here we are converting our resource to node.

* Different Types:
1. Resource → adaptTo(ValueMap.class)
 - Use when: you only need properties of the resource. use when we just want the properties stored on this resource.
 - Eg: Read title, description, custom component fields, Fetch simple configuration values, Access metadata properties quickly.
 - Code eg : ValueMap props = resource.adaptTo(ValueMap.class);
             String title = props.get("jcr:title", String.class);

2. Resource → adaptTo(Node.class)
 - Use when: you need low-level JCR operations.
 - eg: Add/remove nodes manually, Set properties using JCR APIs.
 - Code eg: Node node = resource.adaptTo(Node.class);
            node.setProperty("myProp", "value");

3. Resource → adaptTo(Page.class)
 - Use when: the resource represents a page and you need page-specific methods.
 - eg: Get page title, template, parent page, Navigate site structure, Check if page is valid or published.
 - Code eg: Page page = resource.adaptTo(Page.class);
            String title = page.getTitle();

4. Resource → adaptTo(Asset.class)
 - Use when: the resource represents a DAM asset and you need asset functionality.
 - eg: Read asset metadata, Get renditions (original, thumbnails), Access binary streams, Add new renditions.
 - Code eg: Asset asset = resource.adaptTo(Asset.class);
            String mimeType = asset.getMimeType();

=============================================================================================
*) OverLaying /libs Paths
- The main use of this is to do the modifications in the /libs/(OOTB) components, templates, or any other /libs/(OOTB) nodes. because we should not modify
  the /libs/(OOTB) nodes directly, so for that we can use this overlaying and here we will copy those node or folders to our
  /apps/ path and we do the modifications there.


--> OVERLAYING (or) OVERRIDING THE LIBS NODE (doing in crx, but we can do through code also by pkging it out and putting in our code folder and giving a push)
STEP: 1 - Copy the node which you want to modify eg: "/libs/settings/wcm/designs/default" or /libs/msm/wcm/rolloutconfigs
STEP: 2 - Go to "apps/settings/wcm/designs" or "/apps/msm" node and paste it there and rename the node from default to whatever you want.
STEP: 3 - Remove all the other nodes and folder, which we don't need. and keep the needed ones.
STEP: 4 - Do the modifications needed. and save it.

--> Important point : the path in the /libs/ and the path in the /apps/ must match, means if we are copying the node from "/libs/settings/wcm/designs/default"
    to "/apps/settings/wcm/designs/default", then the path is same, but if we are copying the node from "/libs/settings/wcm/designs/default" to
    "/apps/settings/wcm/mydesign/designs" then in that case the overlayed node wont work.
    Eg : "/libs/msm/wcm/rolloutconfigs" node is copied to "/apps/msm/wcm/rolloutconfigs"

=============================================================================================
*) Understanding -- "GSON"
- Gson is a Java library developed by Google that converts: "Java Object → JSON" called Serialization and "JSON → Java Object" Deserialization.
- Why do we need Gson? : Because Java and JSON are different formats and java cant understand json response directly,
                         and Gson helps us bridge that gap. It allows us to easily convert complex Java objects into JSON strings that
                         can be sent over the network or stored, and also to convert JSON strings back into Java objects for processing.
- Very Important: Field Names Must Match between JSON and Java Class.

- Using GSON
    -> Create a java class
        public class TodoItem {
                                    // the below three fields are the properties of the json response we are getting from the API,
                                    so we have to create the same fields in our java class
            private int id;
            private String todo;
            private boolean completed;

            // getters setters
        }

    -> Then we give
        Gson gson = new Gson();
        TodoItem item = gson.fromJson(response, TodoItem.class);

    -> How Gson Knows Where to Put Data
        - Eg json : {
                        "id": 1,
                        "todo": "Buy groceries",
                      }
        - When Gson see's - gson.fromJson(response, TodoItem.class);, then it goes to that TodoItem class and looks for the fields,
          and then it matches the json properties with the java class fields, and then it put the values in those fields.
        - It compares json property "id" with java class field "private int id;" and then assigns its value, and same for other properties.
        - This complete process is called de-serialization, means converting json response to java object.

EXAMPLE : (IN DEPTH EXPLANATION)
-> API RESPONSE : {
                    "todos": [
                      {
                        "id": 1,
                        "todo": "Learn Java"
                      },
                      {
                        "id": 2,
                        "todo": "Learn Gson"
                      }
                    ]
                  }

- Java Class for above json response : public class TodoResponse {

                                           private List<TodoItem> todos;

                                           public List<TodoItem> getTodos() {
                                               return todos;
                                           }
                                       }

- Java Class for TodoItem : public class TodoItem {
                                    private int id;
                                    private String todo;

                                    // getters and setters
                                }

- Now when this line executes : TodoResponse responseObj = gson.fromJson(jsonString, TodoResponse.class);

     - Gson internall does this :
          1. Creates response object of type TodoResponse. - TodoResponse responseObj = new TodoResponse();
          2. Then it goes to json key "todos" and then finds the matching field in our java class which is "private List<TodoItem> todos;".
          3. then it creates a list internally - List<TodoItem> todoList = new ArrayList<>();
          4. then reads json object {"id": 1, "todo": "Learn Java"} and then creates a TodoItem object - TodoItem item1 = new TodoItem();
          5 then sets the values in that object - item1.setId(1); item1.setTodo("Learn Java");
          6. then adds that item to the list - todoList.add(item1); similarly it does for all the json objects in the array.
          7. now it assigns that list to the response object - responseObj.todos = todoList;
          8. now our "responseObj" contains -- TodoResponse
                                                |
                                                --> todos
                                                      |
                                                      --> TodoItem(id=1,todo="Learn Java")
                                                      --> TodoItem(id=2,todo="Learn Gson")
    - so now when you call: responseObj.getTodos() it returns List<TodoItem> which contains : [
                                                                                               TodoItem(id=1,todo="Learn Java"),
                                                                                               TodoItem(id=2,todo="Learn Gson")
                                                                                              ]


----------------------------------------
- Serialization : converting Java Object → JSON
        - Suppose we have a java object of TodoItem class, and we want to convert it to json string, then we will do like this :
            TodoItem item = new TodoItem();
            item.setId(1);
            item.setTodo("Buy groceries");
            item.setCompleted(false);

            Gson gson = new Gson();
            String jsonString = gson.toJson(item);
        - Here gson.toJson(item) will convert the java object "item" to json string like this :
            {
                "id": 1,
                "todo": "Buy groceries",
                "completed": false
            }

=============================================================================================
*) USING ResourceResolver and Session and modifying the nodes (Basic Codes) (IN-DEPTH ABOUT THIS IN NEXT BLOCK)

- In SERVLET(where the methods give us in-built Request) (So we get a resourceResolver using that request)
Steps:
1 - Getting the "Request"
    ->  doGet(SlingHttpServletRequest request, SlingHttpServletResponse response)

2 - Getting ResourceResolver from the request
    -> ResourceResolver resourceResolver = request.getResourceResolver();

3 - Converting the ResourceResolver to session(something like datatype conversion) (we can only get the session when we have a resourceResolver)
    -> Session session = resourceResolver.adaptTo(Session.class);

4 - Basic page path
    -> String parentPath = "/content/aemgeeks/us/en";

5 - getting hold of the page resource using ResourceResolver
    ->Resource parentResource = resourceResolver.getResource(parentPath);

    * Imp (Applicable only for ResourceType Servlets): Getting Hold Resource Path using "Request".
        Resource resource = request.getResource();
        String resourcePath = resource.getPath();

6 - Converting or Resource to Node, so that we can do the modifications
    -> Node parentNode = parentResource.adaptTo(Node.class);

7 - Doing some node modifications
    -> Node newNode = parentNode.addNode("sampleNode", "nt:unstructured");
                       newNode.setProperty("title", "My Sample Node");
                       newNode.setProperty("createdBy", "Bhargav");
                       newNode.setProperty("year", 2025);

8 - Saving the session
    -> session.save();


--> In sling model we get the "ResourceResolver" using
   @SlingObject
    private ResourceResolver resourceResolver;

=============================================================================================
*** VERY IMPORTANT NOTES **** GETTING ResourceResolver, Resource, Request, Session, Node *** (In-Depth)

IMP POINTS:
> Session always comes from ResourceResolver, and ResourceResolver comes from 1) request, 2) resource, 3) resolverFactory depending on context.
> In Servlets or Sling Models, AEM automatically provides a ResourceResolver because they are request-based (A Sling request gives you a resolver).
> But in Services, Schedulers, there is no request and no automatic context — so AEM doesn’t know who is trying to access the repository.
  in that cases we need "ResourceResolverFactory" to get the ResourceResolver.
> In WF Process --> Using "WorkflowSession" we get the "ResourceResolver", "Session".
> We have to close the "ResourceResolver" after using. ->  resourceResolver.close();,
   - And importantly we have to Close the resolver only when we created it(means resolverFactory.getServiceResourceResolver(...)).
   - Short memory trick ---> Created it → close it :: Framework provided it → do not close it.

---------------------------------------
--> WAYS OF GETTING HOLD OF NODE and DOING READ AND WRITE AND THEN SAVING IT

1) Using ResourceResolver (Common way – read + write)
        - Getting the hold of resource using ResourceResolver
            Resource resource = resourceResolver.getResource("/content/mySite/page/jcr:content");

        - Reading the node properties of that resource
            Node node = resource.adaptTo(Node.class);    //here we are converting our resource to node.
            String title = node.getProperty("jcr:title").getString();

        - writing the node properties of that resource and saving the session.
            Node node = resource.adaptTo(Node.class);
            node.setProperty("jcr:description", "Updated via ResourceResolver");
            node.getSession().save();

---------------------------------------
2) Using JCR Session
        - Getting the session using ResourceResolver
            ResourceResolver resourceResolver = request.getResourceResolver();
            Session session = resourceResolver.adaptTo(Session.class);

        - Reading
            Node node = session.getNode("/content/mySite/page/jcr:content");
            String title = node.getProperty("jcr:title").getString();

        - Writing and saving
            node.setProperty("jcr:description", "Updated via Session");
            session.save();

---------------------------------------
3) In SLING MODEL : in sling model we can directly get the resource, without using ResourceResolver.

    - getting the current resource
            @Inject   (only @inject may give null sometimes, so either use both @inject, @self or use @SlingObject)
            private Resource resource;

    - Getting the Node and reading
            Node node = resource.adaptTo(Node.class);
            String propValue = node.getProperty("jcr:title").getString();

    - Writing and saving
            node.setProperty("jcr:description", desc);
            node.getSession().save();

* Here using @SlingObject also we can get the ResourceResolver, Resource
            @SlingObject
             private ResourceResolver resourceResolver;

---------------------------------------
4) In Servlet :
* The methods like doGet and doPost will in-built give the "SlingHttpServletRequest request" from the request we can get the "ResourceResolver"
    - Getting the ResourceResolver
            protected void doGet(SlingHttpServletRequest request, SlingHttpServletResponse response){
                    ResourceResolver resourceResolver = request.getResourceResolver();

    - Using this ResourceResolver we get the hold of resource
            Resource resource = resourceResolver.getResource("/content/mySite/page/jcr:content");

    - Coverting the available resource to node
            Node node = resource.adaptTo(Node.class);

    - Reading, writing and saving
            String title = node.getProperty("jcr:title").getString();

            node.setProperty("jcr:description", "Updated via Servlet");
            node.getSession().save();

---------------------------------------
5) In Service, Scheduler :
* Here in service we use "ResourceResolverFactory" to get the ResourceResolver.
* Demo for this refer
    1) com/aem/geeks/core/utils/ResolverUtil.java
    2) com/aem/geeks/core/services/impl/ComponentsInfoServiceImpl.java

    - Getting ResourceResolverFactory
            @Reference
            private ResourceResolverFactory resolverFactory;

    - Getting ResourceResolver using ResourceResolverFactory
            final Map<String, Object> paramMap = new HashMap<String, Object>();
            paramMap.put( ResourceResolverFactory.SUBSERVICE, GEEKS_SERVICE_USER );
            ResourceResolver resolver = resourceResolverFactory.getServiceResourceResolver(paramMap);  //here we got the ResourceResolver

    - Getting the resource using ResourceResolver
            Resource resource = resolver.getResource("/content/mySite/page/jcr:content");

    - Converting this resource to node
            Node node = resource.adaptTo(Node.class);

    - Reading, Writing and saving
            String title = node.getProperty("jcr:title").getString();
            node.setProperty("jcr:description", "Updated via OSGi Service");
            node.getSession().save();

-----
* In SERVICE we cannot get the Resource directly
- We get it like this
    - From Sling Model : myService.process(resource);  --> passing the resource through a method parameter.
    - From a Servlet: myService.process(request.getResource());
    - From a wfProcess : Session session = workflowSession.adaptTo(Session.class);
                         ResourceResolver resolver = workflowSession.adaptTo(ResourceResolver.class);
                         Resource resource = resolver.getResource(path);
                         myService.process(resource);

- Using ResourceResolverFactory we can get the resource, But for that we have to hardcode the Current resource path or send the path from model. Service
  cannot fetch the current resource path.

---------------------------------------
6) In Workflow
* Using "WorkflowSession" we get the "ResourceResolver", "Session".
* Reference is in ->  com/aem/geeks/core/workflows/GeeksWorkflowProcess.java

    - Getting the JCR:Session and Payload path
            public void execute(WorkItem workItem, WorkflowSession workflowSession, MetaDataMap processArguments) {
                    Session session = workflowSession.adaptTo(Session.class);   //Got the session
                    WorkflowData workflowData = workItem.getWorkflowData();
                    String path = workflowData.getPayload().toString() + "/jcr:content";   // got the payload resource path as a string

    - Getting Node using session
            Node node = (Node) session.getItem(path);

    - Writing and saving
            node.setProperty("jcr:description", "I am in Workflow");
            session.save();

-------
- Getting Resource in Workflow Process : Session session = workflowSession.adaptTo(Session.class);
                                         ResourceResolver resolver = workflowSession.adaptTo(ResourceResolver.class);
                                         Resource resource = resolver.getResource(payloadPath);

---------------------------------------
7) Special mention - Using Page API
    - Getting hold of resource
          PageManager pageManager=resourceResolver.adaptTo(PageManager.class);
          Page page = pageManager.getPage("/content/mySite/page");
          Resource contentRes = page.getContentResource();  //here we directly got the jcr:content

    - Reading using ValueMap
          ValueMap valueMap = contentRes.getValueMap();  // here it will allow you to read all the properties in that resource node
          String title = valueMap.get("jcr:title", String.class);

    - Reading, Writing and saving using node
          Node node = contentRes.adaptTo(Node.class);
          String title = node.getProperty("jcr:title").getString();
          node.setProperty("jcr:description", "Updated via Page API");
          node.getSession().save();


---------------------------------------
8) Classes or interfaces that helps in Accessing HTTP & External API  (for REST / external systems)
- HttpClient (HttpGet, HttpPost) : Used to make external REST API calls.
- HttpURLConnection (Java standard) : Simplified way to call REST APIs using core Java.
- CloseableHttpClient : Common AEM approach for secure API calls.
- JSONObject / Gson : To parse and handle JSON responses from APIs.

---------------------------------------
--> Getting Node My Understanding

ResourceResolver
│
├──► session ──► Node node = session.getNode("/content/mySite/page/jcr:content"); ──► String title = node.getProperty("jcr:title").getString();
|
├──► Resource ──► Node node = resource.adaptTo(node.class) ──► String title = node.getProperty("jcr:title").getString();


---------------------------------------
--> MAPS - COMPLETE IMPORTANT map showing how Resource, ResourceResolver, Node, Session, and related APIs connect across AEM.

[ ResourceResolverFactory ]
          │
          ▼
 [ ResourceResolver ] ────► provides access to Repository (read/write)
          │
          ├──► getResource("/path/to/node")
          │        │
          │        ▼
          │    [ Resource ]  ← Sling representation of a JCR node
          │        │
          │        ├──► adaptTo(ValueMap) → for reading properties
          │        └──► adaptTo(Node) → for direct JCR access
          │                 │
          │                 ├──► getProperty("jcr:title") → read
          │                 ├──► setProperty("prop", "value") → write
          │                 └──► getSession() → obtain Session
          │
          └──► adaptTo(Session)
                     │
                     ▼
              [ JCR Session ]
                     │
                     ├──► getNode("/path/to/node")
                     ├──► save()   ← commits changes
                     └──► logout() ← closes session

---------------------------------------
--> Common Flows

-> READING
        ResourceResolver → getResource(path)
               ↓
        Resource → adaptTo(Node)
               ↓
        Node → getProperty("propName")

-> WRITING
        ResourceResolver → getResource(path)
               ↓
        Resource → adaptTo(Node)
               ↓
        Node → setProperty("prop", "value")
               ↓
        Node.getSession().save()


-> Just One Line Summary
        ResourceResolverFactory
           ↓
        ResourceResolver
           ↓ getResource()
        Resource
           ↓ adaptTo(Node)
        Node
           ↓ getSession()
        Session.save()

=============================================================================================
--> Debugging Tips
- ClientLibs : you can check which Clientlibs are loading for a specific page, by going to page source and seraching- clientlib name,
               <script (to see what are all clientlibs are loading),

=============================================================================================